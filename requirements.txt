# STDG-NRDEN — see DEPENDENCIES_AND_COMPUTE.md. Also install ONE of:
#   pip install iisignature
#   pip install signatory
# Optional: tensorboard (for --tensorboard)

torch>=2.0,<2.7
torchvision>=0.15
torchaudio>=2.0
numpy>=1.26,<2.1
pandas>=2.0
scipy>=1.9
scikit-learn>=1.3
matplotlib>=3.8
tqdm>=4.67
torchdiffeq>=0.2
